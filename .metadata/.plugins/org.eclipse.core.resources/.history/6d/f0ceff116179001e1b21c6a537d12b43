package Disarium_Number;

public interface Test {
	public static void disariumCheck(int number)
	{
		int temporary=number;
		int digit=0;
		int sum=0;
		while(temporary!=0)
		{
			temporary/=10;
			digit++;
		}
		temporary=number;
		for(int i=0;i<digit;i++)
		{
			sum+=power(temporary%10,digit);
			temporary/=10;
		}
	}
	public static int power(int num,int pow)
	{
		for(int i=1;i<pow;i++)
		{
			num*=num;
		}
		return num;
	}
	
}
