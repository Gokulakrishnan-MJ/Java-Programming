package Disarium_Number;

public interface Test {
	public static void disariumCheck(int number)
	{
		int temporary=number;
		int digit=0;
		int sum=0;
		while(temporary!=0)
		{
			temporary/=10;
			digit++;
		}
		temporary=number;
		for(int i=0;i<digit;i++)
		{
			sum+=temporary%10
		}
	}
	public static power(int num,int pow)
	{
		
	}
	
}
